//check if a alphabet is upperclass


#include<stdio.h>
int main() {
    char ch;
    printf("Enter the character:");
    scanf("%c",&ch);
    if (ch>='A' && ch<= 'Z'){
        printf("Given character is uppercase");
    }
}
